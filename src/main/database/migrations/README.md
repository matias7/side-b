# Database migrations

Future schema changes belong here as numbered, idempotent migrations. The current bootstrap schema remains in `connection.js` until the first versioned migration is introduced.
